apiVersion: v1
kind: Service
metadata:
  name: station-frontend
  labels:
    app: station-frontend
spec:
  type: {{ .Values.service.type }}
  selector:
    app: station-frontend
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 80
