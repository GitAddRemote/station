apiVersion: 1

contactPoints:
  - orgId: 1
    name: station-alerts
    receivers:
      - uid: station-email
        type: email
        settings:
          addresses: ${GF_ALERT_EMAIL:?GF_ALERT_EMAIL is required for alerting}
          singleEmail: false
        disableResolveMessage: false
